# This is ~/.cmake-format.py 
# Maximum line width
line_width = 80

# Always wrap arguments for these functions
always_wrap = {
    "target_link_libraries",
    "add_executable",
    "add_library",
}

